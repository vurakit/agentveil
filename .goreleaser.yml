version: 2

project_name: vura

builds:
  - id: vura-proxy
    main: ./cmd/proxy
    binary: vura-proxy
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

  - id: vura-cli
    main: ./cmd/vura
    binary: vura
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

dockers:
  - image_templates:
      - "ghcr.io/vura-ai/vura:{{ .Version }}"
      - "ghcr.io/vura-ai/vura:latest"
    dockerfile: Dockerfile
    build_flag_templates:
      - "--platform=linux/amd64"
